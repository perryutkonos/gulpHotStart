include head.pug

    
block variables
    
doctype html
html(lang='ru')

    +dev_head(page_title || '', scripts || [])

    body

        include header.pug

        main
            block content

        include footer.pug
